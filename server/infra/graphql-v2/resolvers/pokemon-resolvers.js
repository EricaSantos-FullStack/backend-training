import getPokemons from '../../../use-case/get-pokemons/get-pokemons';

async getPokemons(){
    return
};









// import ad from '../../../domains/ad';
// import getOne from '../../general/get-cycle';

// export default async function createAd(body) {
//   const cycle = await getOne(body.lot);

//   return ad.createAd({ ...body, cycle: cycle && [cycle] });
// }
